# Hugo build artifacts
resources/_gen/
.hugo_build.lock

# Build output (if building from source)
# Note: public/ is currently committed for deployment
# Uncomment if switching to build from source in CI:
# public/
